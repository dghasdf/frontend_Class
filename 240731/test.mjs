//사용자로부터 태어난 년도를 받아라.
//태어난 년도를 기준으로 나이가 몇살인지 #result공간에 출력
//innertest 속성을 활용하면, 특정 node안에 텍스트 문장을 삽입할 수 있다.
let test = "Hi js";
console.log(test);

// const button = document.querySelector("button");
// const calc = () => {
//   const birthYear = Number(prompt("태어난 년도 입력", "ex.2002"));
//   const curuentYear = 2024;
//   const age = curuentYear - birthYear + 1;
//   const result = document.querySelector("#result");
//   result.innerText = `당신의 나이는 ${age}세 입니다`;
// };
// button.addEventListener("click", calc);
