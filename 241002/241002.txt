Home

Result = > 사용자

page routing
- 페이지 구조를 설계할수 있는 컴포넌트 > layout.jsx
- 컴포넌트 > outlet > 자식요소 컴포넌트 생성 및 활용
- 동적파라미터 값을 활용해서 page분기(*나눠서정의)
-app.jsx > 모든 자식컴포넌트들의 최상위 부모컴포넌트역할
-createBrowserRouter:page layout tree
-routerProvider:props 모든 하위 요소 컴포넌트

*외부소스(자료)를 활용해서 무언가의 스타일 적용 ||값을 적용하고자 할때(*img / video)=>

1)src 폴더 해당 값을 가져오는 방법
>import을 통해서 값을 변수로 찾아서 사용

2) public 폴더 해당 값 가져오는 방법
> 절대위치 좌표를 기준으로 문자열형태의 주소값을 사용

최조MBTI 시작하는 단계 => 0단계
> +1 =>12개 // 