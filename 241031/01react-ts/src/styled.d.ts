import styled from "styled-components";

declare module "stykled-components" {
  export interface DefalutTheme {
    bgColor: string;
    textColor: string;
    accentColor: string;
    cardBgColor: string;
  }
}
