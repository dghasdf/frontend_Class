// const arr: number[] = [1, 2, 3];
// const newArr = arr.map((it) => it * 2);
// console.log(newArr);

// 명령형 방식 프로그래밍 코드
// 선언형 방식으로 프로그래밍 


const map =<T>(arr: T)

// 명령형 방식(low-level함수)
const arrTest=[1,2,3];
const result = arrTest.map((item)=> item)
